import styled from 'styled-components';

const Spacing = styled.div`
  height: 30rem;
`;

// const BackgroundOpacity = styled.div`
//   background: rgb(30, 31, 42, 0.1);
// `;

export default Spacing;
